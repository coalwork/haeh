<%
  const errors = {
    404: ['Not Found', 'Sorry, we couldn\'t find that page'],
    401: ['Unauthorized', 'Sorry, you can\'t access this page without authorization'],
    403: ['Forbidden', 'Sorry, you can\'t access this page']
  };
%>

<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>haeh</title>
    <link rel="stylesheet" href="styles/main.min.css">
    <script
      src="https://code.jquery.com/jquery-3.5.1.min.js"
      integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
      crossorigin="anonymous">
    </script>
  </head>
  <body>
    <%
      let strings = {};
      try {
        if (user) strings = user;
      } catch(err) {}
    %>
    <%- include('assets/headernav.html.ejs', strings); %>
    <main>
      <h1 style="font-size: 4em; white-space: nowrap"><%= errorCode %> <%= errors[errorCode][0] %></h1>
      <p style="margin-top: 1em; font-size: 1.2em"><%= errors[errorCode][1] %></p>
    </main>
  </body>
</html>
